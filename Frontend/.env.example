# Production backend API base URL (used when not running `npm run dev`)
# Local dev uses http://localhost:5000/api/v1 automatically.
# No trailing slash on the path segment before /analyze.
VITE_API_URL=https://your-backend.onrender.com/api/v1
